# trash

**Status:** not yet implemented. Lands in **Phase 3**.

## Responsibility
Spec §4 screen 12: restore / permanently delete. Backed by `Documents.isTrashed`/`trashedAt` (already in the Phase 0 schema).

## Contents (planned)
- `trash_screen.dart`, reusing the document list widgets from `features/home`.
